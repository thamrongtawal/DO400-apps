pipeline{
    agent any

    stages{
        stage('Deploy') {
            steps {
                sh '''
                    oc project hvdini-deploy-strategies
                    oc start-build greeting-service --follow --wait
                '''
            }
        }
    }
}
